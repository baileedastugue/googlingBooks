const Book = require('../models/Book');

module.exports = {
    findAll: function(req, res) {
        // Book
        //     .find(req.body.)
        //     .sort({ title: -1 })
        //     .then(books => res.json(books));
    }

}